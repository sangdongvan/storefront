{
  "name": "@storefront/amplify-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "chmod +x ./amplify-prepare.sh && ./amplify-prepare.sh"
  },
  "devDependencies": {
    "@storefront/webapp": "workspace:*",
    "express": "^4.21.1",
    "@rslib/core": "^0.1.1"
  }
}
